package frontend

import llc.ast._

import scala.collection.mutable

object scope {

  /** Provides mappings for every variable declared in this scope. */
  class Scope {
    // private final identMapping: mutable.Map[Int, Type] = new
  }
}
